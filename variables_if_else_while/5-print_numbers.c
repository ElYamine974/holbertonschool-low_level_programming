#include <stdio.h>
/**
* main - Short description, single line
* Return: 0 si le pregramme est bien executé
*/
int main(void)
{
char ch;
for (ch = '0' ; ch <= '9' ; ch++)
{
putchar(ch);
}
putchar('\n');
return (0);
}
